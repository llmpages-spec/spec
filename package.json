{
  "name": "llm-pages-spec-tests",
  "private": true,
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0"
  },
  "scripts": {
    "validate": "ajv -s schemas/*.json -d examples/*.json --spec=draft2020"
  }
}